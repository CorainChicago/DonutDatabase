
  <div class="row-fluid ">
    <h4 class="text-center">Log in with you email account</h4>
    <%= form_tag sessions_path, :html => {:class => "form-inline", role: "form"} do %>
      <div class="form-group">
        <%= label_tag :email%>
        <%= text_field_tag :email, params[:email], :class =>"form-control", :id=>"email" %>
        </div>
      <div class="form-group">
        <%= label_tag :password %>
        <%= password_field_tag :password, :class =>"form-control", :id=>"pwd" %>
      </div>
      <div class="form-group">
        <%= submit_tag "Log in", :class=> "btn btn-default" %></p>
      </div>
    <% end %>
